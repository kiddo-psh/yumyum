# F703 RAG 영양 채팅 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** `POST /ai/chat` 엔드포인트를 구현해 사용자 질문에 식품안전처 DB 기반 RAG 검색 + 개인 식단 컨텍스트를 결합한 영양 Q&A를 제공한다.

**Architecture:** ChromaDB(파일 기반 벡터 DB)에 nutrition_kb.json의 식품 데이터를 인덱싱하고, 질문과 유사한 식품 문서를 검색해 Claude 프롬프트에 주입한다. 요청 바디의 `context` 필드로 개인 식단 수치를 추가 주입해 개인화 답변을 제공한다. `ENV=dev`일 때는 ChromaDB·Claude 호출 없이 mock 응답을 반환한다.

**Tech Stack:** FastAPI, ChromaDB 0.5.23, sentence-transformers 3.3.1 (`paraphrase-multilingual-MiniLM-L12-v2`), pytest, unittest.mock

## Global Constraints

- Python 3.11
- 모든 Claude 호출은 `app.services.claude_service.call_claude()` 경유 — 직접 호출 금지
- `settings.env == "dev"` 시 mock 반환 — 기존 다른 라우터와 동일한 패턴
- 라우터 URL prefix 없음, 데코레이터에 전체 경로 기재 (`/ai/chat`)
- Pydantic v2 (`BaseModel` import from `pydantic`)
- 테스트 파일 위치: `ai/tests/`
- 커밋 메시지 형식: `feat(chat): 설명` / `test(chat): 설명`

---

## File Map

| 파일 | 역할 |
|---|---|
| `ai/requirements.txt` | chromadb, sentence-transformers 추가 |
| `ai/app/data/nutrition_kb.json` | 식품 지식 베이스 (25개 식품) |
| `ai/app/schemas/chat.py` | ChatRequest, ChatResponse, MealContext, ChatSource |
| `ai/app/services/rag_service.py` | ChromaDB 초기화·검색 (싱글턴 컬렉션) |
| `ai/app/services/chat_service.py` | 프롬프트 조립 (순수 함수) |
| `ai/app/routers/ai_chat.py` | POST /ai/chat 라우터 |
| `ai/app/main.py` | ai_chat 라우터 등록 |
| `ai/tests/test_rag_service.py` | rag_service 단위 테스트 |
| `ai/tests/test_chat_service.py` | chat_service 단위 테스트 |
| `ai/tests/test_ai_chat.py` | 라우터 통합 테스트 (mock 모드) |

---

## Task 1: 의존성 추가 & 데이터 & 스키마

**Files:**
- Modify: `ai/requirements.txt`
- Create: `ai/app/data/nutrition_kb.json`
- Create: `ai/app/schemas/chat.py`

**Interfaces:**
- Produces:
  - `ChatRequest(message: str, context: MealContext | None)`
  - `ChatResponse(answer: str, sources: list[ChatSource])`
  - `MealContext(total_protein_g, target_protein_g, total_carb_g, target_carb_g, total_fat_g, target_fat_g, total_kcal, target_kcal, health_goal: str)`
  - `ChatSource(name: str, info: str)`
  - `ai/app/data/nutrition_kb.json` — 각 항목 `{id, name, info, document}` 형태

- [ ] **Step 1: requirements.txt에 의존성 추가**

`ai/requirements.txt` 끝에 두 줄 추가:

```
chromadb==0.5.23
sentence-transformers==3.3.1
```

- [ ] **Step 2: 의존성 설치**

```bash
cd ai
pip install chromadb==0.5.23 sentence-transformers==3.3.1
```

Expected: 설치 완료 (sentence-transformers가 torch를 함께 설치함, 시간 소요)

- [ ] **Step 3: data 디렉토리 생성 후 nutrition_kb.json 작성**

`ai/app/data/nutrition_kb.json` 생성:

```json
[
  {
    "id": "food_001",
    "name": "닭가슴살",
    "info": "100g당 단백질 23g, 열량 109kcal, 지방 2g",
    "document": "닭가슴살 100g: 열량 109kcal, 단백질 23g, 지방 2g, 탄수화물 0g. 고단백 저지방 식품으로 다이어트·근육 증가에 적합하다."
  },
  {
    "id": "food_002",
    "name": "달걀",
    "info": "1개(50g)당 단백질 6g, 열량 74kcal",
    "document": "달걀 1개(50g): 열량 74kcal, 단백질 6.3g, 지방 5g, 탄수화물 0.6g. 완전 단백질 공급원으로 필수 아미노산을 모두 포함한다."
  },
  {
    "id": "food_003",
    "name": "두부",
    "info": "100g당 단백질 8g, 열량 76kcal",
    "document": "두부 100g: 열량 76kcal, 단백질 8g, 지방 4g, 탄수화물 2g. 식물성 단백질 공급원으로 칼슘이 풍부하다."
  },
  {
    "id": "food_004",
    "name": "연어",
    "info": "100g당 단백질 20g, 열량 208kcal, 오메가3 풍부",
    "document": "연어 100g: 열량 208kcal, 단백질 20g, 지방 13g, 탄수화물 0g. 오메가3 지방산이 풍부해 심혈관 건강에 좋다."
  },
  {
    "id": "food_005",
    "name": "참치(통조림)",
    "info": "100g당 단백질 25g, 열량 116kcal",
    "document": "참치(통조림, 물) 100g: 열량 116kcal, 단백질 25g, 지방 1g, 탄수화물 0g. 저지방 고단백 식품으로 간편하게 섭취 가능하다."
  },
  {
    "id": "food_006",
    "name": "소고기(안심)",
    "info": "100g당 단백질 21g, 열량 143kcal",
    "document": "소고기 안심 100g: 열량 143kcal, 단백질 21g, 지방 6g, 탄수화물 0g. 철분과 아연이 풍부한 동물성 단백질 공급원이다."
  },
  {
    "id": "food_007",
    "name": "그릭요거트",
    "info": "100g당 단백질 10g, 열량 59kcal, 칼슘 풍부",
    "document": "그릭요거트(무가당) 100g: 열량 59kcal, 단백질 10g, 지방 0.4g, 탄수화물 3.6g. 유산균과 칼슘이 풍부하며 단백질 함량이 일반 요거트보다 2배 높다."
  },
  {
    "id": "food_008",
    "name": "새우",
    "info": "100g당 단백질 18g, 열량 85kcal",
    "document": "새우 100g: 열량 85kcal, 단백질 18g, 지방 1g, 탄수화물 1g. 저칼로리 고단백으로 콜레스테롤이 다소 높다."
  },
  {
    "id": "food_009",
    "name": "현미밥",
    "info": "100g당 탄수화물 34g, 열량 156kcal, 식이섬유 풍부",
    "document": "현미밥 100g: 열량 156kcal, 단백질 3g, 지방 1g, 탄수화물 34g, 식이섬유 1.8g. 혈당 지수가 낮고 식이섬유가 풍부해 포만감이 오래 지속된다."
  },
  {
    "id": "food_010",
    "name": "흰쌀밥",
    "info": "100g당 탄수화물 37g, 열량 168kcal",
    "document": "흰쌀밥 100g: 열량 168kcal, 단백질 3g, 지방 0.3g, 탄수화물 37g. 소화가 빠르고 혈당 지수가 현미밥보다 높다."
  },
  {
    "id": "food_011",
    "name": "고구마",
    "info": "100g당 탄수화물 27g, 열량 114kcal, 베타카로틴 풍부",
    "document": "고구마 100g: 열량 114kcal, 단백질 2g, 지방 0.1g, 탄수화물 27g, 식이섬유 2.5g. 베타카로틴과 식이섬유가 풍부하고 혈당 지수가 낮다."
  },
  {
    "id": "food_012",
    "name": "감자",
    "info": "100g당 탄수화물 17g, 열량 77kcal",
    "document": "감자 100g: 열량 77kcal, 단백질 2g, 지방 0.1g, 탄수화물 17g. 칼륨이 풍부하고 포만감이 높다. 튀기면 열량이 급격히 증가한다."
  },
  {
    "id": "food_013",
    "name": "오트밀",
    "info": "100g(건조)당 탄수화물 67g, 열량 379kcal, 베타글루칸 풍부",
    "document": "오트밀(건조) 100g: 열량 379kcal, 단백질 13g, 지방 7g, 탄수화물 67g, 식이섬유 10g. 베타글루칸이 콜레스테롤 감소에 효과적이고 포만감이 높다."
  },
  {
    "id": "food_014",
    "name": "바나나",
    "info": "1개(120g)당 탄수화물 28g, 열량 107kcal",
    "document": "바나나 1개(120g): 열량 107kcal, 단백질 1.3g, 지방 0.4g, 탄수화물 28g. 칼륨과 마그네슘이 풍부해 운동 후 전해질 보충에 적합하다."
  },
  {
    "id": "food_015",
    "name": "퀴노아",
    "info": "100g(조리)당 단백질 4g, 탄수화물 21g, 열량 120kcal",
    "document": "퀴노아(조리) 100g: 열량 120kcal, 단백질 4g, 지방 2g, 탄수화물 21g. 9가지 필수 아미노산을 포함한 완전 단백질 식물성 식품이다."
  },
  {
    "id": "food_016",
    "name": "아보카도",
    "info": "100g당 지방 15g(불포화), 열량 160kcal",
    "document": "아보카도 100g: 열량 160kcal, 단백질 2g, 지방 15g, 탄수화물 9g. 단일불포화지방산이 풍부해 심혈관 건강에 좋고 비타민 E, 칼륨이 많다."
  },
  {
    "id": "food_017",
    "name": "아몬드",
    "info": "30g(한 줌)당 지방 14g, 단백질 6g, 열량 170kcal",
    "document": "아몬드 30g: 열량 170kcal, 단백질 6g, 지방 14g, 탄수화물 6g. 불포화지방산과 비타민 E가 풍부하고 마그네슘이 많아 뼈 건강에 좋다."
  },
  {
    "id": "food_018",
    "name": "호두",
    "info": "30g당 오메가3 2.5g, 지방 18g, 열량 185kcal",
    "document": "호두 30g: 열량 185kcal, 단백질 4g, 지방 18g, 탄수화물 4g. 식물성 오메가3(ALA)가 풍부해 뇌 건강과 항염증 효과가 있다."
  },
  {
    "id": "food_019",
    "name": "시금치",
    "info": "100g당 철분 2.7mg, 열량 23kcal, 저칼로리",
    "document": "시금치 100g: 열량 23kcal, 단백질 2.9g, 지방 0.4g, 탄수화물 3.6g, 철분 2.7mg. 철분, 엽산, 비타민 K가 풍부한 저칼로리 채소다."
  },
  {
    "id": "food_020",
    "name": "브로콜리",
    "info": "100g당 비타민C 89mg, 열량 34kcal, 저칼로리",
    "document": "브로콜리 100g: 열량 34kcal, 단백질 2.8g, 지방 0.4g, 탄수화물 7g. 비타민 C, 칼슘, 식이섬유가 풍부하고 항산화 물질 설포라판을 함유한다."
  },
  {
    "id": "food_021",
    "name": "당근",
    "info": "100g당 베타카로틴 8mg, 열량 41kcal",
    "document": "당근 100g: 열량 41kcal, 단백질 0.9g, 지방 0.2g, 탄수화물 10g. 베타카로틴이 매우 풍부해 눈 건강과 면역력 강화에 도움이 된다."
  },
  {
    "id": "food_022",
    "name": "사과",
    "info": "1개(200g)당 탄수화물 26g, 열량 104kcal, 식이섬유 풍부",
    "document": "사과 1개(200g): 열량 104kcal, 단백질 0.5g, 지방 0.3g, 탄수화물 26g, 식이섬유 4.4g. 펙틴이 장 건강에 좋고 폴리페놀이 항산화 작용을 한다."
  },
  {
    "id": "food_023",
    "name": "우유",
    "info": "200ml당 단백질 6g, 칼슘 220mg, 열량 122kcal",
    "document": "우유 200ml: 열량 122kcal, 단백질 6g, 지방 6.4g, 탄수화물 9.6g, 칼슘 220mg. 칼슘 흡수율이 높아 뼈 건강에 최적이고 완전 영양식품이다."
  },
  {
    "id": "food_024",
    "name": "콩(대두)",
    "info": "100g당 단백질 36g(건조), 열량 446kcal",
    "document": "대두(건조) 100g: 열량 446kcal, 단백질 36g, 지방 20g, 탄수화물 30g. 식물성 완전 단백질로 이소플라본이 호르몬 균형에 도움을 준다."
  },
  {
    "id": "food_025",
    "name": "소간",
    "info": "100g당 철분 5mg, 단백질 20g, 열량 130kcal",
    "document": "소간 100g: 열량 130kcal, 단백질 20g, 지방 4g, 탄수화물 5g, 철분 5mg. 헴철 함량이 매우 높아 철분 결핍성 빈혈 개선에 효과적이다."
  }
]
```

- [ ] **Step 4: chat.py 스키마 작성**

`ai/app/schemas/chat.py` 생성:

```python
from pydantic import BaseModel
from typing import Optional, List


class MealContext(BaseModel):
    total_protein_g: float
    target_protein_g: float
    total_carb_g: float
    target_carb_g: float
    total_fat_g: float
    target_fat_g: float
    total_kcal: float
    target_kcal: float
    health_goal: str  # DIET | MUSCLE | HEALTH | DISEASE


class ChatRequest(BaseModel):
    message: str
    context: Optional[MealContext] = None


class ChatSource(BaseModel):
    name: str
    info: str


class ChatResponse(BaseModel):
    answer: str
    sources: List[ChatSource]
```

- [ ] **Step 5: 커밋**

```bash
git add ai/requirements.txt ai/app/data/nutrition_kb.json ai/app/schemas/chat.py
git commit -m "feat(chat): F703 의존성·지식베이스·스키마 추가"
```

---

## Task 2: rag_service.py (ChromaDB 초기화·검색)

**Files:**
- Create: `ai/app/services/rag_service.py`
- Test: `ai/tests/test_rag_service.py`

**Interfaces:**
- Consumes:
  - `ai/app/data/nutrition_kb.json` — `{id, name, info, document}` 항목 배열
- Produces:
  - `search(query: str, n_results: int = 3) -> list[dict]`
    - 반환 형태: `[{"name": str, "info": str, "document": str}]`
  - `reset_collection_for_test()` — 테스트용 컬렉션 초기화 함수

- [ ] **Step 1: 테스트 작성**

`ai/tests/test_rag_service.py` 생성:

```python
from unittest.mock import patch, MagicMock
import app.services.rag_service as rag_service


def _make_mock_collection(docs, names, infos):
    """ChromaDB collection mock 생성 헬퍼."""
    col = MagicMock()
    col.count.return_value = len(docs)
    col.query.return_value = {
        "documents": [docs],
        "metadatas": [[{"name": n, "info": i} for n, i in zip(names, infos)]],
    }
    return col


def test_search_returns_list_of_dicts():
    mock_col = _make_mock_collection(
        docs=["닭가슴살 100g: 열량 109kcal, 단백질 23g..."],
        names=["닭가슴살"],
        infos=["100g당 단백질 23g, 열량 109kcal"],
    )
    with patch.object(rag_service, "_get_collection", return_value=mock_col):
        result = rag_service.search("단백질 많은 음식")

    assert isinstance(result, list)
    assert len(result) == 1
    assert result[0]["name"] == "닭가슴살"
    assert result[0]["info"] == "100g당 단백질 23g, 열량 109kcal"
    assert "document" in result[0]


def test_search_passes_query_to_collection():
    mock_col = _make_mock_collection(
        docs=["시금치 100g: 철분 2.7mg..."],
        names=["시금치"],
        infos=["100g당 철분 2.7mg, 열량 23kcal"],
    )
    with patch.object(rag_service, "_get_collection", return_value=mock_col):
        rag_service.search("철분 많은 음식", n_results=3)

    mock_col.query.assert_called_once()
    call_kwargs = mock_col.query.call_args
    assert "철분 많은 음식" in call_kwargs.kwargs.get("query_texts", call_kwargs.args[0] if call_kwargs.args else [])


def test_search_returns_empty_when_no_docs():
    mock_col = MagicMock()
    mock_col.count.return_value = 0
    mock_col.query.return_value = {"documents": [[]], "metadatas": [[]]}
    with patch.object(rag_service, "_get_collection", return_value=mock_col):
        result = rag_service.search("아무거나")

    assert result == []


def test_search_n_results_capped_by_collection_count():
    """n_results > 컬렉션 크기일 때 에러 없이 동작해야 한다."""
    mock_col = _make_mock_collection(
        docs=["두부 100g..."],
        names=["두부"],
        infos=["100g당 단백질 8g"],
    )
    mock_col.count.return_value = 1
    with patch.object(rag_service, "_get_collection", return_value=mock_col):
        result = rag_service.search("단백질", n_results=10)

    assert len(result) == 1
    # n_results=min(10, 1)=1 로 query가 호출됐는지 확인
    call_kwargs = mock_col.query.call_args
    passed_n = call_kwargs.kwargs.get("n_results") or call_kwargs.args[1]
    assert passed_n == 1
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

```bash
cd ai
pytest tests/test_rag_service.py -v
```

Expected: `ModuleNotFoundError: No module named 'app.services.rag_service'`

- [ ] **Step 3: rag_service.py 구현**

`ai/app/services/rag_service.py` 생성:

```python
import json
from pathlib import Path

import chromadb
from chromadb.utils import embedding_functions

_collection = None

_KB_PATH = Path(__file__).parent.parent / "data" / "nutrition_kb.json"
_CHROMA_PATH = str(Path(__file__).parent.parent.parent / "chroma_db")
_MODEL_NAME = "paraphrase-multilingual-MiniLM-L12-v2"


def _get_collection():
    global _collection
    if _collection is not None:
        return _collection

    ef = embedding_functions.SentenceTransformerEmbeddingFunction(
        model_name=_MODEL_NAME
    )
    client = chromadb.PersistentClient(path=_CHROMA_PATH)
    col = client.get_or_create_collection("nutrition", embedding_function=ef)

    if col.count() == 0:
        _load_kb(col)

    _collection = col
    return _collection


def _load_kb(collection) -> None:
    with open(_KB_PATH, encoding="utf-8") as f:
        items = json.load(f)
    collection.add(
        documents=[item["document"] for item in items],
        ids=[item["id"] for item in items],
        metadatas=[{"name": item["name"], "info": item["info"]} for item in items],
    )


def search(query: str, n_results: int = 3) -> list[dict]:
    """
    쿼리와 의미적으로 유사한 식품 문서를 ChromaDB에서 검색한다.
    반환: [{"name": str, "info": str, "document": str}]
    """
    collection = _get_collection()
    count = collection.count()
    if count == 0:
        return []

    results = collection.query(
        query_texts=[query],
        n_results=min(n_results, count),
        include=["documents", "metadatas"],
    )
    docs = results["documents"][0]
    metas = results["metadatas"][0]
    return [
        {"name": m["name"], "info": m["info"], "document": d}
        for d, m in zip(docs, metas)
    ]
```

- [ ] **Step 4: 테스트 실행 → 통과 확인**

```bash
pytest tests/test_rag_service.py -v
```

Expected:
```
PASSED tests/test_rag_service.py::test_search_returns_list_of_dicts
PASSED tests/test_rag_service.py::test_search_passes_query_to_collection
PASSED tests/test_rag_service.py::test_search_returns_empty_when_no_docs
PASSED tests/test_rag_service.py::test_search_n_results_capped_by_collection_count
```

- [ ] **Step 5: 커밋**

```bash
git add ai/app/services/rag_service.py ai/tests/test_rag_service.py
git commit -m "feat(chat): F703 rag_service ChromaDB 검색 구현 (TDD)"
```

---

## Task 3: chat_service.py (프롬프트 조립)

**Files:**
- Create: `ai/app/services/chat_service.py`
- Test: `ai/tests/test_chat_service.py`

**Interfaces:**
- Consumes:
  - `MealContext` from `app.schemas.chat`
  - `docs: list[dict]` — rag_service.search() 반환값
- Produces:
  - `build_prompt(message: str, docs: list[dict], context: MealContext | None) -> str`

- [ ] **Step 1: 테스트 작성**

`ai/tests/test_chat_service.py` 생성:

```python
from app.services.chat_service import build_prompt
from app.schemas.chat import MealContext


def _make_context(**kwargs) -> MealContext:
    defaults = dict(
        total_protein_g=39.0, target_protein_g=120.0,
        total_carb_g=150.0,  target_carb_g=250.0,
        total_fat_g=30.0,    target_fat_g=60.0,
        total_kcal=900.0,    target_kcal=2000.0,
        health_goal="MUSCLE",
    )
    defaults.update(kwargs)
    return MealContext(**defaults)


def test_prompt_contains_message():
    prompt = build_prompt("단백질 많은 음식 뭐야?", docs=[], context=None)
    assert "단백질 많은 음식 뭐야?" in prompt


def test_prompt_contains_doc_names():
    docs = [
        {"name": "닭가슴살", "info": "100g당 단백질 23g", "document": "..."},
        {"name": "두부",     "info": "100g당 단백질 8g",  "document": "..."},
    ]
    prompt = build_prompt("단백질", docs=docs, context=None)
    assert "닭가슴살" in prompt
    assert "두부" in prompt


def test_prompt_contains_context_values():
    ctx = _make_context(total_protein_g=39.0, target_protein_g=120.0)
    prompt = build_prompt("오늘 단백질 충분한가요?", docs=[], context=ctx)
    assert "39" in prompt
    assert "120" in prompt


def test_prompt_health_goal_translated_to_korean():
    ctx = _make_context(health_goal="DIET")
    prompt = build_prompt("질문", docs=[], context=ctx)
    assert "다이어트" in prompt


def test_prompt_no_context_section_when_context_is_none():
    prompt = build_prompt("닭가슴살 칼로리?", docs=[], context=None)
    assert "사용자 오늘 식단" not in prompt


def test_prompt_no_docs_section_when_docs_empty():
    prompt = build_prompt("질문", docs=[], context=None)
    assert "참고 식품 정보" not in prompt


def test_prompt_all_health_goals_translate():
    for goal, expected in [
        ("DIET",    "다이어트"),
        ("MUSCLE",  "근육 증가"),
        ("HEALTH",  "건강 유지"),
        ("DISEASE", "질환 관리"),
    ]:
        ctx = _make_context(health_goal=goal)
        prompt = build_prompt("질문", docs=[], context=ctx)
        assert expected in prompt, f"{goal} → {expected} 번역 실패"
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

```bash
pytest tests/test_chat_service.py -v
```

Expected: `ModuleNotFoundError: No module named 'app.services.chat_service'`

- [ ] **Step 3: chat_service.py 구현**

`ai/app/services/chat_service.py` 생성:

```python
from app.schemas.chat import MealContext

_HEALTH_GOAL_KR = {
    "DIET":    "다이어트",
    "MUSCLE":  "근육 증가",
    "HEALTH":  "건강 유지",
    "DISEASE": "질환 관리",
}


def build_prompt(message: str, docs: list[dict], context: MealContext | None) -> str:
    """RAG 검색 결과와 개인 식단 컨텍스트를 합쳐 Claude 프롬프트를 조립한다."""
    lines = [
        "당신은 영양 전문 AI 코치입니다. "
        "아래 정보를 바탕으로 친절하고 구체적으로 답변하세요.\n"
    ]

    if docs:
        lines.append("[참고 식품 정보]")
        for d in docs:
            lines.append(f"- {d['name']}: {d['info']}")
        lines.append("")

    if context:
        goal_kr = _HEALTH_GOAL_KR.get(context.health_goal, context.health_goal)
        lines.append("[사용자 오늘 식단]")
        lines.append(f"- 단백질: {context.total_protein_g}g / 목표 {context.target_protein_g}g")
        lines.append(f"- 탄수화물: {context.total_carb_g}g / 목표 {context.target_carb_g}g")
        lines.append(f"- 지방: {context.total_fat_g}g / 목표 {context.target_fat_g}g")
        lines.append(f"- 칼로리: {context.total_kcal}kcal / 목표 {context.target_kcal}kcal")
        lines.append(f"- 건강 목표: {goal_kr}")
        lines.append("")

    lines.append(f"[질문]\n{message}")
    return "\n".join(lines)
```

- [ ] **Step 4: 테스트 실행 → 통과 확인**

```bash
pytest tests/test_chat_service.py -v
```

Expected:
```
PASSED tests/test_chat_service.py::test_prompt_contains_message
PASSED tests/test_chat_service.py::test_prompt_contains_doc_names
PASSED tests/test_chat_service.py::test_prompt_contains_context_values
PASSED tests/test_chat_service.py::test_prompt_health_goal_translated_to_korean
PASSED tests/test_chat_service.py::test_prompt_no_context_section_when_context_is_none
PASSED tests/test_chat_service.py::test_prompt_no_docs_section_when_docs_empty
PASSED tests/test_chat_service.py::test_prompt_all_health_goals_translate
```

- [ ] **Step 5: 커밋**

```bash
git add ai/app/services/chat_service.py ai/tests/test_chat_service.py
git commit -m "feat(chat): F703 chat_service 프롬프트 조립 구현 (TDD)"
```

---

## Task 4: 라우터 & main.py 등록

**Files:**
- Create: `ai/app/routers/ai_chat.py`
- Modify: `ai/app/main.py` (line 2, line 15)
- Test: `ai/tests/test_ai_chat.py`

**Interfaces:**
- Consumes:
  - `search(query, n_results)` from `app.services.rag_service`
  - `build_prompt(message, docs, context)` from `app.services.chat_service`
  - `call_claude(prompt, max_tokens)` from `app.services.claude_service`
  - `ChatRequest`, `ChatResponse`, `ChatSource` from `app.schemas.chat`
  - `settings.env` from `app.config`
- Produces:
  - `POST /ai/chat` → `ChatResponse`

- [ ] **Step 1: 테스트 작성**

`ai/tests/test_ai_chat.py` 생성:

```python
import pytest
from fastapi.testclient import TestClient
from unittest.mock import patch, AsyncMock
from app.main import app

client = TestClient(app)


def test_chat_mock_mode_returns_200():
    """ENV=dev(기본값)일 때 mock 응답 반환."""
    resp = client.post("/ai/chat", json={"message": "단백질 많은 음식 추천해줘"})
    assert resp.status_code == 200
    body = resp.json()
    assert "answer" in body
    assert isinstance(body["sources"], list)


def test_chat_mock_response_contains_mock_marker():
    resp = client.post("/ai/chat", json={"message": "아무거나"})
    assert "[MOCK]" in resp.json()["answer"]


def test_chat_with_context_mock_mode():
    payload = {
        "message": "오늘 단백질 충분한가요?",
        "context": {
            "total_protein_g": 39.0,
            "target_protein_g": 120.0,
            "total_carb_g": 150.0,
            "target_carb_g": 250.0,
            "total_fat_g": 30.0,
            "target_fat_g": 60.0,
            "total_kcal": 900.0,
            "target_kcal": 2000.0,
            "health_goal": "MUSCLE",
        },
    }
    resp = client.post("/ai/chat", json=payload)
    assert resp.status_code == 200
    body = resp.json()
    assert "answer" in body
    assert "sources" in body


def test_chat_missing_message_returns_422():
    resp = client.post("/ai/chat", json={})
    assert resp.status_code == 422


def test_chat_sources_have_name_and_info():
    resp = client.post("/ai/chat", json={"message": "test"})
    sources = resp.json()["sources"]
    for s in sources:
        assert "name" in s
        assert "info" in s


def test_chat_prod_mode_calls_rag_and_claude():
    """ENV=prod 시 rag_service.search와 call_claude가 호출된다."""
    with patch("app.routers.ai_chat.settings") as mock_settings, \
         patch("app.routers.ai_chat.rag_service.search", return_value=[
             {"name": "닭가슴살", "info": "100g당 단백질 23g", "document": "..."}
         ]) as mock_search, \
         patch("app.routers.ai_chat.call_claude", new_callable=AsyncMock,
               return_value="닭가슴살을 드세요!") as mock_claude:
        mock_settings.env = "prod"
        resp = client.post("/ai/chat", json={"message": "단백질 음식 추천"})

    assert resp.status_code == 200
    mock_search.assert_called_once_with("단백질 음식 추천")
    mock_claude.assert_called_once()
```

- [ ] **Step 2: 테스트 실행 → 실패 확인**

```bash
pytest tests/test_ai_chat.py -v
```

Expected: `ImportError` 또는 `404 Not Found` (라우터 미등록)

- [ ] **Step 3: ai_chat.py 라우터 구현**

`ai/app/routers/ai_chat.py` 생성:

```python
from fastapi import APIRouter
from app.schemas.chat import ChatRequest, ChatResponse, ChatSource
from app.services.claude_service import call_claude
from app.services import rag_service, chat_service
from app.config import settings

router = APIRouter(tags=["AI Chat"])


@router.post("/ai/chat", response_model=ChatResponse)
async def chat(req: ChatRequest):
    """F703 - RAG 기반 영양 채팅. 식품안전처 DB 검색 + 개인 식단 컨텍스트 주입."""
    if settings.env == "dev":
        return _mock_response(req.message)

    try:
        docs = rag_service.search(req.message)
    except Exception:
        docs = []

    prompt = chat_service.build_prompt(req.message, docs, req.context)
    answer = await call_claude(prompt, max_tokens=500)
    sources = [ChatSource(name=d["name"], info=d["info"]) for d in docs]
    return ChatResponse(answer=answer, sources=sources)


def _mock_response(message: str) -> ChatResponse:
    return ChatResponse(
        answer="[MOCK] 닭가슴살과 두부를 추가하면 단백질 목표 달성에 도움이 됩니다.",
        sources=[
            ChatSource(name="닭가슴살", info="100g당 단백질 23g, 열량 109kcal"),
            ChatSource(name="두부",     info="100g당 단백질 8g, 열량 76kcal"),
        ],
    )
```

- [ ] **Step 4: main.py에 라우터 등록**

`ai/app/main.py` 수정:

```python
from fastapi import FastAPI
from app.routers import ai_meal, ai_plan, ai_routine, food, ai_report, ai_coach, ai_chat

app = FastAPI(
    title="냠냠코치 AI Server",
    description="Spring Boot ↔ FastAPI AI 연동 서버",
    version="1.0.0",
)

app.include_router(ai_meal.router)
app.include_router(ai_plan.router)
app.include_router(ai_routine.router)
app.include_router(food.router)
app.include_router(ai_report.router)
app.include_router(ai_coach.router)
app.include_router(ai_chat.router)


@app.get("/health", tags=["Health"])
def health():
    return {"status": "ok", "service": "nyamnyam-fastapi"}
```

- [ ] **Step 5: 테스트 실행 → 통과 확인**

```bash
pytest tests/test_ai_chat.py -v
```

Expected:
```
PASSED tests/test_ai_chat.py::test_chat_mock_mode_returns_200
PASSED tests/test_ai_chat.py::test_chat_mock_response_contains_mock_marker
PASSED tests/test_ai_chat.py::test_chat_with_context_mock_mode
PASSED tests/test_ai_chat.py::test_chat_missing_message_returns_422
PASSED tests/test_ai_chat.py::test_chat_sources_have_name_and_info
PASSED tests/test_ai_chat.py::test_chat_prod_mode_calls_rag_and_claude
```

- [ ] **Step 6: 전체 테스트 통과 확인**

```bash
pytest tests/ -v --ignore=tests/test_gms_api.py --ignore=tests/test_mfds_api.py
```

Expected: 모든 기존 테스트 + 신규 테스트 PASSED

- [ ] **Step 7: 커밋**

```bash
git add ai/app/routers/ai_chat.py ai/app/main.py ai/tests/test_ai_chat.py
git commit -m "feat(chat): F703 POST /ai/chat 라우터 구현 + main.py 등록"
```
